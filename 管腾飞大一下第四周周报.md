# web刷题内容(主要平台nss)

### 交流会md5复习

<details>
  <summary>奇妙的md5</summary>
    具体解决方案讨论会都讲了  
    
    这里总结一下知识点  
    
    题目需要类似“or 1”的md5值  

    输入ffifdyop  
    
    在检查中找到php代码  

    用若比较和get方式  

    强比较用数组  

    解决
</details>

### upload

<details>
  <summary>easyupload</summary>
    配置一句话木马文件<?php @eval($_POST['cmd']); ?>  

    .php文件上传失败，尝试其他后缀  
    
    （这有两个解题分支）

    - 使用一定不会被列入黑名单的后缀，如jpg然后用bp拦截换后缀名

    - 猜不会被列入的可执行php文件，如php3，php4，php5，phtml，pht，phps，inc

    后面使用剑蚁就行  
    

</details>

<details>
  <summary>babyuploads</summary>
  没提示，所有php文件后缀都被排除  

  这里原本想尝试bp方法，但是f12看到作者提示/source  

  在url后输入下载到源文件  

  <details><summary>反应错误原因</summary>  
  
```python
  if "." in file.filename:
        return "Bad filename!", 403
```

不给用后缀
</details>


解题关键：
```python
@app.route('/file/<id>')
def file(id):
    conn = db()
    cur = conn.cursor()
    cur.execute("select path from files where id=?", (id,))
    res = cur.fetchone()
    if res is None:
        return "File not found", 404

    # print(res[0])

    with open(os.path.join("uploads/", res[0]), "r") as f:
        return f.read()
```

说明需要flag文件的id

```python
file.save('uploads/' + file.filename)
    return redirect('/file/' + uid)

```

说明需要把文件名变成flag  

代码中添加的/uploads路径  

应用漏洞：os.path.join中若后面的路径以/开头，则后面的路径变成绝对路径  

需要打造/flag文件名，需要用bp

filename参数变为/flag  

得到flag文件的id  

NSSCTF{d3222d13-4215-4d75-8f65-10d8a8a3843f}  
  
</details>


### php

<details><summary>checkin</summary>
  题目直接显示php文件  

  正常GET方法赋值失败  

  （?ahahahaha=jitanglailo&cuishiyuan=NISCTF）  

  根据题目自带的提示  

  （尝试复制源码看看->010editor）  

  条件发生变化  

  ```python
"jitanglailo" == $_GET[ahahahaha] &??+!!??& "?? Flag!??N1SACTF" == $_GET[??Ugeiwo??cuishiyuan])
  ```

 重新配GET方法，但是要换符号  

 配出ahahahaha=jitanglailo&%E2%80%AE%E2%81%A6Ugeiwo%E2%81%A9%E2%81%A6cuishiyuan=%E2%80%AE%E2%81%A6%20Flag!%E2%81%A9%E2%81%A6N1SACTF

  

  




</details>
